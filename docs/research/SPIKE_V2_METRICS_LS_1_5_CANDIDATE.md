# Spike V2 指标过滤研究候选

状态：仅离线研究候选；不得修改线上 Spike 默认策略或启用 live 交易。

## 候选参数

在以下固定 v2 基线上，仅保留：

```text
strategy = trading_platform.strategies.spike.v2_1:V21
max_oi_change_pct = 0
max_ls_ratio = 1.5
entry_tier_mode = tier3-only
exit_policy = candidate-v1
total_notional = 1000
warmup_hours = 168
prior_high_lookback_hours = 6
rise_low_lookback_hours = 168
min_rise_duration_hours = 24
profit_unlock_percent = 1.5
chunk_hours = 4320
```

`profit_unlock_percent=1.5` 是本研究基线的一部分，不能误用 `V21` 声明的默认值 `3`。

基线之上保留两个仅用于离线验证的 profile：收益优先的 `L` 使用
`max_consecutive_up_minutes=0`；风险优先的 `C4` 使用
`max_consecutive_up_minutes=4`。二者都不是线上默认参数。

## 已有证据

全周期参数矩阵 `reports/spike-v2-metrics-sweep-available-time` 覆盖
2025-08-01 至 2026-08-01、92 个交易对、552 个任务、13 workers。未过滤基线为
93 笔、胜率 72.04%、净收益 3349.768U、PF 3.083；`LS=1.5, OI=0` 为
73 笔、胜率 75.34%、净收益 3888.235U、PF 11.058。

时间切分 `reports/spike-v2-metrics-time-robustness-2026h1` 覆盖
2026-02-01 至 2026-08-01、92 个交易对、368 个任务、13 workers：

| OI 上限 | LS 上限 | 交易 | 胜率 | 净收益 U | PF | 总亏损 U |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 0 | 59 | 71.19% | 2265.342 | 4.418 | 662.852 |
| 15 | 0 | 59 | 71.19% | 2265.342 | 4.418 | 662.852 |
| 0 | 1.5 | 47 | 72.34% | 2298.361 | 8.467 | 307.800 |
| 15 | 1.5 | 47 | 72.34% | 2298.361 | 8.467 | 307.800 |

在该切分的 89 个基线信号中，17 个最近可见的 5m 指标快照满足 `LS > 1.5`，与
LS 组缺失的信号集合一致；所有信号的 OI 5m 变化均不超过 9.21456%，所以 OI=15
没有产生过滤。故 `max_oi_change_pct=15` 不纳入候选。

## 连阳与 LS 交互审计

为验证连阳过滤是否能与 `LS=1.5` 共同降低 XNY 类路径风险，已完成独立报告
`reports/spike-v2-consecutive-ls-audit`：2025-08-01 至 2026-08-01、92 个固定交易对、
368 个任务、13 个 worker。四臂均固定为 `V21`、`tier3-only`、`candidate-v1`、
`profit_unlock_percent=1.5`、`total_notional=1000`、`max_oi_change_pct=0` 和
`chunk_hours=4320`；92/92 个交易对均完成，未因数据覆盖被跳过。

| 臂 | 连阳上限 | LS 上限 | 交易 | 胜率 | 净收益 U | PF | 保守净收益 U | 总亏损 U |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| B（基线） | 0 | 0 | 93 | 72.04% | 3349.768 | 3.083 | 3180.602 | 1608.340 |
| L（仅 LS） | 0 | 1.5 | 73 | 75.34% | 3888.235 | 11.058 | 3813.503 | 386.577 |
| C（仅连阳） | 3 | 0 | 73 | 71.23% | 3401.865 | 4.311 | 3401.865 | 1027.529 |
| CL（组合） | 3 | 1.5 | 60 | 76.67% | 3676.954 | 11.790 | 3676.954 | 340.767 |

`L` 相对 `B` 少 20 笔交易、净收益增加 `538.466U`，保守净收益增加
`632.901U`。`CL` 相对 `L` 再少 13 笔，净收益减少 `211.281U`、保守净收益减少
`136.549U`；其更高的胜率和 PF 不足以使其在本期收益上支配 `L`。

### 信号归因

- `B` 记录了 130 条核心条件通过的触发信号。
- `L` 记录 103 条触发和 27 条 `max_ls_ratio` 拒绝；其中对应基线的 20 笔实际成交
  合计 `-538.466U`，与 `L` 相对基线的净收益改善精确一致。它同时过滤了盈利和亏损
  交易，但本期被移除集合的净额为负。
- `C` 记录 102 条触发和 30 条 `max_consecutive_up_minutes` 拒绝；对应基线的 21 笔
  已成交交易合计反而为 `+566.384U`，但总结果只比基线高 `52.097U`。
- `CL` 记录 83 条触发和 49 条拒绝（仅连阳 22、仅 LS 19、二者同时 8）；对应基线的
  34 笔已成交交易合计为 `+291.295U`，而组合总结果仍比基线高 `327.186U`。

后两项说明连阳会改变后续候选信号与持仓路径，不能把最终结果误读为“直接删掉坏单”。
LS 拒绝事件均记录了 `metrics_available_time`；该报告中没有快照晚于信号时刻，最大的
历史可见性滞后为 298 秒，未发现向前读取指标的证据。

### 尾部风险与 XNY

| 臂 | 最大已实现亏损 | 最大浮亏 | 全仓风险标记 | 最低风险比 |
| --- | --- | --- | ---: | ---: |
| B | FLUX `-535.001U` | XNY `-1282.156U` | 1/93（1.08%） | 0.779936 |
| L | FF `-117.932U` | XNY `-1282.156U` | 1/73（1.37%） | 0.779936 |
| C | 1000000BOB `-297.854U` | 1000000BOB `-354.991U` | 0/73 | 2.816974 |
| CL | FF `-117.932U` | AKE `-336.425U` | 0/60 | 2.972435 |

风险比为 `1 / abs(max_adverse_return)`；小于等于 1 时记录全仓风险标记。它是基于
最不利 1 秒价格的路径诊断，当前引擎不会据此强制平仓。因此 XNY 在 `B`/`L` 于
2025-12-04 17:54:06 UTC 的交易最终仍记录为 `+338.254U`，但曾有 `-1282.156U`
最大浮亏，不能当作可承受的真实盈利。

`C`/`CL` 并不是简单删除 XNY：连阳=3 分别拒绝了连续 6 分钟、9 分钟的早期候选，
随后在 18:01:32 UTC 出现新信号并成交，最终为 `+618.480U`、最大浮亏
`-315.449U`、没有全仓风险标记。`LS=1.5` 没有过滤 XNY 的早期候选。

GIGGLE 在四臂均为 0 信号、0 成交；它已纳入且数据完整，但没有走到核心入场审计点。
这份报告不能作为任一过滤器规避 GIGGLE 风险的证据。

## 连阳阈值敏感性与时间切片

为避免把 `连阳=3` 当作唯一可选值，已完成以下两轮冻结矩阵。两轮均固定
`LS=1.5`、OI 关闭、V21、相同退出和资金参数，并使用 13 个 worker；92/92 个交易对均
完成且没有数据不完整的已选标的。

- 全周期 `reports/spike-v2-ls-consecutive-sensitivity`：2025-08-01 至
  2026-08-01，`连阳=0..6`，644/644 成功。
- 时间切片 `reports/spike-v2-ls-consecutive-2026h1-validation`：2026-02-01 至
  2026-08-01，冻结比较 `0/3/4/5`，368/368 成功。该区间在 XNY 风险事件之后，用于检查
  连阳阈值的收益代价；其中没有新的全仓风险标记，不能单独证明风险泛化。

全周期中，`4` 是风险路径仍被移除时收益损失最小的候选：

| 连阳上限 | 交易 | 净收益 U | PF | 保守净收益 U | 总亏损 U | 最大浮亏 | 全仓风险标记 |
| ---: | ---: | ---: | ---: | ---: | ---: | --- | ---: |
| 0（L） | 73 | 3888.235 | 11.058 | 3813.503 | 386.577 | XNY `-1282.156U` | 1 |
| 3 | 60 | 3676.954 | 11.790 | 3676.954 | 340.767 | AKE `-336.425U` | 0 |
| 4（C4） | 65 | 3835.246 | 11.498 | 3835.246 | 365.330 | AKE `-336.425U` | 0 |
| 5 | 68 | 3834.948 | 10.920 | 3760.216 | 386.577 | AKE `-336.425U` | 0 |
| 6 | 71 | 3667.262 | 10.486 | 3592.530 | 386.577 | XNY `-1282.156U` | 1 |

`C4` 相对 `L` 的全周期净收益少 `52.989U`（1.36%），但保守净收益多
`21.743U`，且将最大浮亏从 XNY 的 `-1282.156U` 降到 AKE 的 `-336.425U`。`5` 的原始
净收益只比 `C4` 低 `0.298U`，但保守收益低 `75.030U`；`3` 比 `C4` 少赚
`158.292U`。`6` 会放行 XNY 的连续 6 根阳线早期信号，故不能作为风险候选。

XNY 在 `1..5` 均改为 18:01:32 UTC 的后续信号成交，最终 `+618.480U`、最大浮亏
`-315.449U`；`0` 和 `6` 都在 17:54:06 UTC 的早期信号入场，触发全仓风险标记。这里的
边界来自策略的严格大于比较：连续阳线数等于 6 时只有上限小于 6 才会拒绝。

H1 的冻结切片没有复现 XNY 类风险，故主要回答收益代价：

| 连阳上限 | 交易 | 胜率 | 净收益 U | PF | 总亏损 U | 相对 L 净收益 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0（L） | 47 | 72.34% | 2298.361 | 8.467 | 307.800 | 0 |
| 3 | 39 | 74.36% | 1988.987 | 8.428 | 267.782 | -309.374 |
| 4（C4） | 41 | 73.17% | 2056.790 | 8.178 | 286.553 | -241.571 |
| 5 | 43 | 69.77% | 2035.543 | 7.613 | 307.800 | -262.818 |

H1 中 `C4` 拒绝了 6 笔 L 的实际成交，四赢两亏、合计 `+241.571U`，正好等于其相对 L 的
收益差。因此 C4 不是收益过滤器；它是以可量化收益代价换取历史 XNY 路径保护的风险覆盖层。
在风险 profile 内，C4 比 3 多赚 `67.803U`，比 5 多赚 `21.247U`，且三者的 H1 最大浮亏
相同，故将 C4 固定为后续风险验证候选。

## 结论边界

- LS=1.5 的 H1 改善由移除 12 笔已成交交易形成，净移除 -33.018U；其中
  `1000000BOBUSDT` 的 -297.854U 占避免亏损的 83.9%。剔除该单后，LS 相对基线少赚
  264.835U。因此它是降低单一极端亏损暴露的研究候选，不是已证实的稳健收益或爆仓过滤。
- H1 区间已参与过参数探索，不是独立盲测；没有 LS 阈值敏感性曲线，也没有组合资金竞争或
  内部 K 线缺口的完整验证。
- C4 的 H1 是针对连阳阈值的后事件时间切片，不含新的全仓风险路径；它验证了收益代价，
  不能证明 C4 能泛化规避下一次 XNY/GIGGLE 类事件。
- 当时的两个历史报告运行在拒绝信号审计修复之前，`all_signals.csv` 只含
  `signal_triggered`，并未持久化 OI/LS 拒绝原因。上述 17 条结论由归档指标按同一
  `available_time` 规则复算，不能把旧 CSV 误读为逐条拒绝审计。

## 后续门禁

后续回测会在所有核心入场条件通过后记录 `signal_rejected`，保留连阳、OI、LS 和
涨幅/成交量上限的实际阈值与原始指标。升级该研究候选前，必须完成：

1. 在未参与选参的时间段重放，并核对 `all_signals.csv` 中的逐条拒绝审计。
2. 对指标和 K 线内部缺口做完整性检查，并给出组合级资金竞争与风险统计。
3. 处理 live runtime 的 `metrics_5m` 数据供给能力并完成独立执行验证。

当前 live runtime 会拒绝需要 `metrics_5m` 的策略，且 live 模式本身仍受退出策略校准
门禁保护；本文件不构成任何上线授权。
