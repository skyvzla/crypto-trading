# 阶段 1：策略核心

## 目标

将阶段 0 已确认的 1s 触发、本地价格计划和交易轮次规则实现为可确定回放的核心。

## 工作

- 完成 1s Bar 和必要辅助数据的回放；
- 实现监听租约接入后的策略计算；
- 实现固定名义金额、三档入场、部分成交、失效、900 秒和信号切换；
- 为已确认规则补充固定案例和回放测试。

## 不做

- 不接交易所订单；
- 不接清算地图；
- 不实现 Web 控制和其他策略。

## 退出条件

相同输入得到一致结果，同一时间不会出现重叠交易轮次，所有已确认生命周期规则都有测试。
