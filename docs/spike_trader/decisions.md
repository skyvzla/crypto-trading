# 决策记录

本文件只记录用户已经确认的方向。没有确认的内容放在“待确认”中，不作为实现依据。

## 已确认

| 编号 | 决策 |
|---|---|
| D-001 | V1 只做上涨尖峰后的做空，其他策略暂缓 |
| D-002 | 策略以完成的 1s Bar 为准，不消费 tick |
| D-003 | 清算地图当前暂空；未来只能作为策略内部的可选辅助，缺失时不得阻止开仓 |
| D-004 | 同一时间只允许一个交易轮次，交易中不叠加其他信号 |
| D-005 | 每轮使用固定总名义金额 `N USDT`，三档按 30% / 40% / 30% 分配，杠杆不能扩大总额 |
| D-006 | 触发失效条件时撤销未成交入场单；已成交仓位继续退出 |
| D-007 | 持仓从第一笔实际成交起计时；900 秒时净收益不为正则退出 |
| D-008 | 盈利仓位不因 900 秒直接退出，使用止盈、动能衰减或回撤等方式管理 |
| D-009 | 盈利且超过 900 秒出现新有效信号时，先平旧轮次再开新轮次，不允许重叠 |
| D-010 | 浏览器只控制 subcategory 交易池准入，不热更新策略参数 |
| D-011 | subcategory 关闭时撤销相关未成交入场单；已有仓位继续保护和退出 |
| D-012 | 采用发现扫描和监听租约，不监听全市场；租约交易对才进入常规 1s 监听 |
| D-013 | 运行模式只有 replay、testnet、live，不提供实时纸盘模式 |
| D-014 | 不同策略类型使用不同交易所账户；采用“策略类型 = 进程 = 专用账户”，账户映射由部署配置完成，程序不实现共享账户的复杂策略路由 |
| D-015 | subcategory 准入刷新复用可交易池的发现扫描周期，不设置独立高频轮询；扫描周期由 D-021 冻结为 5 分钟 |
| D-016 | 做空盈利管理的第一检查位置为 `origin_price`（起涨点，即价格下跌至该价或更低）；到达后若下跌动能没有明显衰减则继续持仓，若明显衰减则减半仓 |
| D-017 | 到达起涨点并减半后，剩余仓位结合时间周期、趋势和动能退出；计算定义和阈值确认前不得实现 |
| D-018 | 持仓前 90 秒不设置固定价格止损；90 秒后同时使用时间风险与动能风险，任一达到退出条件即立即退出，且两类条件均随持仓时间逐步收严 |
| D-019 | 下跌动能衰减定义为下跌速度逐步放缓；应采用可解释的专业指标计算，具体指标组合与阈值先用回测标定再冻结 |
| D-020 | 24 小时作为持仓风险复核节点而非硬上限；价格仍处于确认的下跌通道时允许继续持有，突破并站稳 5m 或 15m 趋势线时全部退出；“下跌通道”“站稳”和趋势线算法待标定 |
| D-021 | 可交易池每 5 分钟扫描一次；subcategory 准入复用该 5 分钟扫描节拍 |
| D-022 | 交易周期状态使用 Redis 存储；任一交易周期存续期间全局禁止开启新交易周期，D-009 的先平后开仍适用 |
| D-023 | `SUBMIT_UNKNOWN` 先按每 5 秒查询一次、最多 12 次进行 testnet 验证；达到上限仍未知时继续 fail-closed，不自动重下单 |
| D-024 | Redis Campaign 仅在交易所订单全部终态且仓位归零后释放，不增加人工确认或固定冷却时间；恢复与异常路径继续 fail-closed |
| D-025 | 动能研究先测试“归一化 log-price 快慢斜率 + MACD histogram + ADX/DI”组合，形成阈值对照后再冻结生产参数 |
| D-026 | 预挂接仓单是策略核心：三档做空入场必须提前使用 SELL LIMIT 挂在交易所，避免触价后再由程序下单产生延迟；止盈不预挂，在时间、趋势或动能条件满足后再执行退出 |
| D-027 | 退出指标窗口和阈值不要求用户凭经验直接确认；先作为带版本的候选参数用于 replay/testnet，依据历史回测、walk-forward、真实成交偏差和运行样本迭代，证据达标后再冻结生产版本；候选参数不得自动进入 live |
| D-028 | 当前策略冻结为 `candidate-v1` 观察基线；暂停参数搜索、收益寻优和新规则猜测。必须先向用户展示逐轮买卖点、成交、费用、PnL、价格路径和指标快照，共同确认具体问题后才可进行单变量实验；执行安全、恢复和账本一致性测试不受此限制 |
| D-029 | Web 面板不实现身份认证、登录和角色权限；部署限定在内网访问，由网络边界承担访问控制。审计字段（`updated_by`、`reason`）保留但由调用方自行填写，不作为身份凭证 |
| D-030 | Binance USD-M 交易对元数据每日同步到 PostgreSQL；退市冻结窗口默认 15 天并可由 `SPIKE_DELISTING_FREEZE_DAYS` 配置。非 `PERPETUAL`、非 `TRADING`、字段异常或 `deliveryDate` 距当前不超过窗口的交易对禁止新开仓并撤销未成交入场单；已有仓位仍允许保护性退出 |
| D-031 | 交易对事实与 Binance Category/Subcategory 由独立数据层 worker 每日同步，并提供同源手动 CLI。交易对全局开关不可被策略绕过；策略分类开关是可选黑名单，空配置不影响交易池，任一匹配分类显式关闭即阻止新开仓 |
| D-032 | Web 收益日历不并入绩效分析；运行总览展示当月摘要并下钻到独立完整页，完整页不额外占侧栏一级菜单 |
| D-033 | Web 分类管理统一展示程序同步的 Category/Subcategory 及交易对关联，不提供人工创建、重命名、移动、删除或关联编辑 |
| D-034 | Web 策略风控首期只管理按策略配置的 Category/Subcategory 可选黑名单准入开关，不新增策略参数热更新、高级风险限额或复杂规则编辑器 |

## 数据事实与执行安全约束

- 2026-08-07 对账证实 AKEUSDT DuckDB 的 1s 序列相对 1m/5m/15m Kline 整体早
  8 小时。旧 replay、legacy 对照和退出标定不再构成证据，已归档为无效。
- 历史修正必须显式配置并写入报告元数据，不允许 loader 自动推断时区偏移。
- Kline 只更新指标状态；入场仍为预挂 LIMIT，退出意图只由完成 1s Bar 以当时可执行价产生，
  防止 replay 用 Kline close 成交而 live 用实时价格成交。
- 现有 candidate-v1 参数来源已失效，只能继续用于执行链路验证和逐轮数据观察；
  依 D-028 完成人工评审和后续证据门禁前，不得冻结为生产参数或进入 live。

### 重要：Binance USD-M 永续合约下架信号（2026-08-08 实测）

> **不得遗漏。** 策略只交易 Binance USD-M 永续合约；`/fapi/v1/exchangeInfo`
> 的 `deliveryDate` 是合约下架/结算的重要防线，不能只检查 `status`。

- 正常可交易的永续合约（当日 569 个，包括 `BTCUSDT`、`AKEUSDT`）均为
  `status=TRADING`，`deliveryDate=2100-12-25T08:00:00Z` 的远期默认值。
- 当年具有实际 `deliveryDate` 的 58 个永续合约全部为 `status=SETTLING`，即已退出
  正常交易；Binance 仍会在 `exchangeInfo` 保留它们作为结算记录。
- `HFTUSDT`、`VANRYUSDT`、`ACXUSDT`、`VICUSDT` 均在
  `2026-08-07T09:00:00Z`（17:00 CST）进入该状态。该组是本规则的直接样本。
- 当前快照中不存在 `status=TRADING` 且 `deliveryDate` 非远期默认值的永续合约。单次
  快照不能证明 Binance 会在下架前多久更新此字段，因此不能把它当成唯一的预告来源。

后续 live/testnet 准入必须同时校验 `contractType=PERPETUAL`、`status=TRADING` 和
`deliveryDate` 距当前时间超过退市冻结窗口。窗口默认 15 天，可通过
`SPIKE_DELISTING_FREEZE_DAYS` 配置；交易所元数据每日同步到 PostgreSQL，并在每次
5 分钟安全扫描时按当前 UTC 时间重算。发现近期 `deliveryDate` 时立即停止新开仓并撤销
未成交入场单，已有仓位只允许保护性退出。公告采集用于更早冻结，`exchangeInfo` 的
`status` 与 `deliveryDate` 用作不可绕过的硬校验。

## 已冻结脚本基线（2026-08-06 确认）

以下参数直接来自 `scripts/backtest_dynamic_spike.py`，已确认为唯一事实源，不得擅自修改：

| 参数 | 值 | 说明 |
|---|---|---|
| **触发条件** | | |
| 5 秒涨幅阈值 | 5% | `rise_5s >= 0.05` |
| 5 秒成交量倍数 | 3× | `volume_5s / (median_volume × 5) >= 3.0` |
| 12 小时低点涨幅 | 20% | `close / low_12h - 1 >= 0.20` |
| **价格计算** | | |
| 起涨点窗口（origin） | 16 小时 | 1m K 线最近 960 根的最低价 |
| spike_high 窗口 | 30 分钟 | 1m K 线最近 30 根的最高价 |
| ATR 周期 | 14 | 5m K 线，14 周期 TR 均值 |
| **三档价格公式** | `spike_high - atr × (1.15, 0.75, 0.35)` | `range(3)` 即 n=0,1,2 |
| 第一档（最低价） | `spike_high - 1.15×atr` | 深度回调才触及 |
| 第二档（主力） | `spike_high - 0.75×atr` | 中等回调，40% 权重 |
| 第三档（最高价） | `spike_high - 0.35×atr` | 浅回调优先成交 |
| 三档名义金额权重 | 30% / 40% / 30% | |
| origin 下限保护 | `origin × 1.10` | 三档价格不得低于此值 |
| **失效价** | `max(spike_high + 3.5×atr, 主目标位 + 2.0×atr)` | |
| **订单窗口** | | |
| 订单激活时间 | 触发 Bar 后 1 秒 | `signal_time + 1000ms` |
| 订单 TTL | 180 秒 | 从激活时间算起 |
| 信号冷却时间 | 180 秒 | 两次信号最小间隔 |

### 与脚本的有意偏差（已记录，Phase 0 必须对账）

| 项 | 脚本实现 | 新代码实现 | 原因 |
|---|---|---|---|
| spike_high 计算 | 包含信号所在分钟的 1m K 线（可能未完成） | 只用已完成 1m K 线 + 已缓存 1s Bar | 消除未来数据泄漏 |
| ATR 计算 | 包含信号所在 5m 周期（可能未完成） | 只用已完成 5m K 线 | 消除未来数据泄漏 |

以上偏差会导致与原 CSV 逐笔存在可解释差异（如触发时机提前几秒、三档价格略有不同），属正常现象。Phase 0 必须建立固定测试案例对账。

### 脚本中的未实现项（Phase 2 范围）

- 部分成交：脚本全部成交，新代码支持 LIMIT 跨多根穿价 1s Bar 部分成交及 TTL/撤单
- 持仓计时：脚本从第一笔成交计时，新代码已记录首笔成交时间及审计事件
- 900 秒规则：脚本未实现，仅记录窗口价格
- 手续费：脚本未扣除；新引擎区分 LIMIT Maker 与 MARKET Taker 手续费，但完整净 PnL 结算待退出规则确认
- 盈利管理：脚本未实现

### 当前 replay 安全行为

- 默认加载交易开始前 16 小时数据用于指标预热，预热期间禁止产生订单；
- 缺少 aggTrade、1m 或 5m 必需数据时拒绝运行；
- 未确认期末结算规则前，结束时仍有仓位会保留为 `OPEN`，仅按最后 1s Bar
  计算未实现盈亏，不伪造平仓交易；
- 多币种 replay 使用全局准入锁，同一时间只允许一个币种持有活跃信号、挂单或仓位。
- replay 报告包含信号触发、入场计划、失效、撤单和首笔成交等基础策略审计事件。

## 待确认

- 监听租约的发现条件、回吐定义、确认次数和最大期限；扫描周期已冻结为 5 分钟；
- 到达 `origin_price` 时，下跌速度衰减所采用的专业指标、观察窗口和阈值；
- 5m/15m 趋势线、下跌通道及“突破站稳”的精确定义；24 小时后仍在确认下跌通道中可继续持有；
- 当前已实现 PostgreSQL、Redis、FastAPI、Compose 和“执行层为库”的基线；是否将其冻结为 V1 技术方案；
- 清算地图真实接口结果及其唯一辅助决策点；
- 部分成交、订单触价、手续费、滑点和同秒事件的最终回测口径。
- 三档均为做空限价单时，“仅最高价第三档成交”与价格穿透关系矛盾；需确认档位命名或成交语义。

## 归档规则线索（非现行决策）

归档的 `specifications/SPIKE_STRATEGY_SPEC.md` 曾提出：前 90 秒不设固定价格止损，
90 秒后采用从宽到严的时间衰减价格保护，并结合动能和高位横盘形态退出。该文档同时
明确所有阈值均为候选值，必须回测标定后冻结。

初始版本的 `scripts/append_dynamic_spike_pnl.py` 则采用 `invalid_price` 止损、2R 目标和
900 秒窗口，是另一套实验口径。两者相互冲突，均不得直接作为当前实现依据。
