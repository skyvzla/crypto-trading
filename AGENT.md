# 用户偏好与项目约定

- 默认使用中文回复；较大改动先给出简短计划，再执行并汇报验证结果。
- 有任何不确认的地方，请询问并提出推荐意见，不要随意猜测代码或内容。
- 测试优先使用 Docker Compose 的隔离测试服务，不依赖宿主机环境；完成全量测试、代码审查和 `git diff --check`。
- 发布流程：提交代码后执行安全部署；部署前备份并保留排空/门禁，部署后检查 Compose 状态、健康检查、归档心跳和部署状态。
- PostgreSQL 是业务持久化数据库，不保留 SQLite/双数据库兼容分支；优先使用成熟库或 ORM，避免重复手写基础设施。
- Redis 只用于实时行情、短期数据和任务队列；DuckDB 是 K 线历史归档，单写入归档器，其他进程只读。
- 回测只读 DuckDB 历史归档，不读 Redis 最新数据、不联网补 K 线、不回写归档；归档允许有几分钟延迟，缺失数据明确拒绝。
- 信号确认与下单解耦：风控通过后交给独立 worker/job；网络错误分类、连续重试，不能阻塞信号循环。
- 测试网数据可重置、清理历史输赢和仓位；限价开仓允许短时挂单，超时或趋势失效再撤单。
- 代码保持 KISS/YAGNI/DRY/SOLID：删除垃圾和过时代码，保持底层存储变更不影响业务层；修复后补回归测试。
- 图表和详情应从统一数据层取数；已有历史数据即使标记 stale 也要展示，并明确提示覆盖状态。币种 subcategory 放在详情弹窗中，表格保持简洁。
- 功能点完成后进行测试，测试没有问题提交代码